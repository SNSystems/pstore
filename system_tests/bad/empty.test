# %binaries = the directories containing the executable binaries
# %stores = a directory in which data stores may be created
# %S = the test source directory

XFAIL:*
RUN: "%binaries/pstore-write" "%S/empty"

#eof:empty.test
